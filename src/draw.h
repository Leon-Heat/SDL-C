#include "common.h"

//	Extern Variable
extern declare game;
extern second count;
extern Entity player;
extern Entity emily;
extern Entity ani;
extern Entity *ani1;

//	util.h
extern int Count(clock_t t, int d2);
extern int interval(Entity *k, int frame_max, int seconds);

